const express = require('express');
const app = express();
const port = 8080;

// app.get('/', (req, res) => res.send(' THIS IS CSC 314 CLOUD COMPUTING DEVOPS; CI-CD PIPELINE CREATEDWITH AWS. THIS PAGE INDICATES SUCCESS.YOURNAME: DAVID OKAFORSMATRIC NUMBER: VUG/CSC/21/5191'));

app.listen(port);
console.log(`App running on http://localhost:${port}`);
